export type ProjectType = {
    id: number
    name: string,
    full_name: string,
    description: string,
    icons?: string,
    homepage?: string,
    html_url: string,
    image?: string,
    priority: number,
}